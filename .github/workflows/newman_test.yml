name: Newman Run

on:
  pull_request:
    branches:
    - master

jobs:
  newman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: matt-ball/newman-action@master
        with:
            collection: /Postman%20Collections/Scope-Controller.json
            environment: idporten_systest.postman_environment.json

